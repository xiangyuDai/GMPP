% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GRM.R
\name{GRM}
\alias{GRM}
\title{Calculate additive and dominant genomic relationship matrix}
\usage{
GRM(geno, method = c("additive", "dominant"))
}
\arguments{
\item{geno}{Numeric matrix of genotypes coded as \code{{0, 1, 2}}
(individuals in rows, SNPs in columns).}

\item{method}{Character vector specifying which GRM(s) to compute.
One or both of \code{"additive"} and \code{"dominant"}.}
}
\value{
A named list with components:
  \describe{
    \item{Ga}{Additive GRM (if \code{"additive"} \%in\% method).}
    \item{Gd}{Dominant GRM (if \code{"dominant"} \%in\% method).}
  }
}
\description{
Calculate additive and dominant genomic relationship matrix
}
\examples{
geno <- matrix(sample(0:2, 200 * 50, replace = TRUE), 200, 50)
G <- GRM(geno)
G <- GRM(geno, "additive")
G <- GRM(geno, c("additive", "dominant"))

}
